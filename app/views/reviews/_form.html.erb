<%= form_for([@book, @review], remote: true) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, 'error') %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.number_field :rating, :placeholder => 'rate from 1 to 5', :in => 1..5, class:'form-control', required: true %>
  </div>
    <br>

  <div class="field">
    <%= f.text_area :comment, :placeholder => 'comment', class:'form-control'  %>
  </div>
    <br>

  <div class="actions">
      <%= f.submit 'Save', type: 'submit', class:'btn btn-success' %>
  </div>
<% end %>