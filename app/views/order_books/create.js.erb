<% if @order.errors.any? || @order_book.errors.any? %>
alert("not valid.");
<% else %>
$(".cart-icon").html("<%= j(render 'shared/cart_icon') %>");
$('.cart-icon').fadeOut(100).fadeIn(100).fadeOut(70).fadeIn(50);
<% end %>